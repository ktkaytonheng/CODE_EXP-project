import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: "#000000",
    primary: "black",
    secondary: "#black",
    error: "#f13a59",
    accent: "black",
  },
};
